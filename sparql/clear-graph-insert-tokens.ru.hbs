# clear-graph-insert-tokens.ru.hbs

# Clears a graph and inserts the tokens as the data
# This file uses http://handlebarsjs.com/
# The resulting document is SPARQL Update (tested with AllegroGraph)

CLEAR {{#if graph-uri}}SILENT GRAPH <{{{graph-uri}}}>{{else}}DEFAULT{{/if}};

INSERT DATA {
    {{#if graph-uri~}}
        GRAPH <{{{graph-uri}}}>{
    {{~/if}}
    {{#each tokens~}}
        {{{this}}}
    {{~/each}}
    {{#if graph-uri~}}
        }
    {{~/if}}
}
